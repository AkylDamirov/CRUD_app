version: '3.9'

services:
  app:
    build: .
    volumes:
      - .:/django
    ports:
      - 8001:8001
    image: app:django
    container_name: crud_app
    command: python3 manage.py runserver 0.0.0.0:8001